# name: Deploy to GitHub Pages

# on:
#   push:
#     branches:
#       - main   # Set the branch you want to deploy from (usually 'main')

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v3

#     # - name: Setup Node.js
#     #   uses: actions/setup-node@v3
#     #   with:
#     #     node-version: '18'  # Specify the Node.js version you're using

#     # - name: Install dependencies
#     #   run: |
#     #     npm ci # Use npm ci for clean install (if using npm)
      
#     - name: Build project
#       run: |
#         make build # Run your build command, e.g. npm run build (modify if needed)
      
#     - name: Deploy to GitHub Pages
#       uses: peaceiris/actions-gh-pages@v4
#       with:
#         github_token: ${{ secrets.GITHUB_TOKEN }}
#         publish_dir: ./dist  # Set this to the directory where your static files are generated

